spring:
  application:
    name: KotlinSpringTemplate
  datasource:
    url: jdbc:mysql://localhost:3306/classicmodels?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    show-sql: true